post:
  description: Transform a table using a given table structure
  summary: Transform
  operationId: transform
  parameters:
    - name: tableStructureId
      in: query
      required: true
      schema:
        type: string
  responses:
    '200':
      description: "Table transformed."
    '404':
      description: "Table structure not found."
